# --- Compiled binaries (built in place from the .c sources; regenerate with make/gcc) ---
mod_obstruct
hunt
hunt_noopt
palhunt
palhunt_gmp
mitm_probe
mitm_probeB
congru_probe
check_d5
check_d7
check_d9
check_d21
check_survivors
test_omp
test_mod_threads
mod_dbg
mod-test
mod_obstruct_test
mod_obstruct_bkup
a.out

# --- Zig experiment binaries (2026-06-06; the .zig/.c sources ARE tracked) ---
palhunt_zig
palhunt_c
palhunt_opt_c
palhunt_opt_zig
.zig-cache/
zig-cache/
zig-out/

# --- Object / build artifacts ---
*.o
*.obj

# --- Runtime / checkpoint state ---
*.ckpt

# --- Logs (transient run output; keep a specific one with `git add -f logs/<name>`) ---
logs/

# --- Editor / code-navigation tooling ---
tags
cscope.out
.vimrc
*.swp
*~

# --- Scratch / session / backup sources & data (local only) ---
claude_*_session*.txt
*_session_ref.txt
mod_obstruct_bkup.c
test_omp.c
test_mod_threads.c
endsN5.txt

# --- Claude Code local state ---
.claude/

# Generated 80-col print artifacts (render_print.sh)
docs/doc_txt/

# Personal correspondence — contains name/email, must not reach the
# public mirrors (GitLab / GitHub / Codeberg)
PrimeCurios.txt
PrimePuzzles.txt
outreach.txt

# --- Credentials — MUST NEVER reach the public mirrors ---
# Was untracked but NOT ignored: a `git add -A` would have committed
# it and pushed a live token to GitLab / GitHub / Codeberg.
gitlab-token.txt
*token*.txt

# --- palsplit / palbrute binaries and lock (sources ARE tracked) ---
palsplit
palbrute
palcurve
palsplit_g2
palsplit_g1000
palbrute.lock
test_curve
test_curve_gmp
test_palpred
check_d5
check_d7
check_d9
check_survivors
check_d21
hunt

# --- Python bytecode ---
__pycache__/
